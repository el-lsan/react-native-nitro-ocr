///
/// HybridNitroOcrSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `OCRResult` to properly resolve imports.
namespace margelo::nitro::nitroocr { struct OCRResult; }
// Forward declaration of `RecognizeOptions` to properly resolve imports.
namespace margelo::nitro::nitroocr { struct RecognizeOptions; }

#include "OCRResult.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include "RecognizeOptions.hpp"
#include <optional>
#include <vector>

namespace margelo::nitro::nitroocr {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NitroOcr`
   * Inherit this class to create instances of `HybridNitroOcrSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNitroOcr: public HybridNitroOcrSpec {
   * public:
   *   HybridNitroOcr(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNitroOcrSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNitroOcrSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNitroOcrSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<OCRResult>> recognize(const std::string& source, const std::optional<RecognizeOptions>& options) = 0;
      virtual std::shared_ptr<Promise<std::string>> recognizeText(const std::string& source, const std::optional<RecognizeOptions>& options) = 0;
      virtual std::shared_ptr<Promise<std::vector<std::string>>> getSupportedLanguages() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NitroOcr";
  };

} // namespace margelo::nitro::nitroocr
